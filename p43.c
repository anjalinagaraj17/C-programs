#include<stdio.h>
int main()
{

    int a=5,b=5,c=10;
    (a>c)&&(b>c)?(printf(" a and b are greater than c\n")):(printf(" a and b are not greater than c\n"));
    (a<c)&&(b<c)?(printf("a and b are smaller than c\n")):(printf("a and b are not smaller than c\n"));
    return 0;
}
